# Bao cao loi ngay 16/05/2026

Ghi chu ten file: nguoi dung yeu cau `16/05/2026.md`, nhung tren Windows ky tu `/` la dau phan tach thu muc nen khong hop le trong ten file. File nay duoc tao voi ten tuong duong `16-05-2026.md`.

## 1. Hien trang loi

- Maven ban dau chay bang JDK 17 trong khi `pom.xml` yeu cau Java 21, dan den loi `invalid target release: 21`.
- Cau hinh Mockito tu nhanh `han` them `mock-maker-subclass`, trong khi test dung `mockStatic(...)`, nen static mock khong chay.
- Khi ep chay JDK 25 voi cau hinh cu, JaCoCo/Mockito cu khong tuong thich day du voi class file Java 25.
- IntelliJ bao `JDK "ms-21" is missing`, lam `module-info.java` bi do ca o `requires java.sql;`. Day la loi Project SDK cua IDE, khong phai loi `java.sql`.
- MySQL JDBC driver la automatic module `mysql.connector.j`; neu chay theo module path nen khai bao ro trong `module-info.java`.

## 2. Huong sua

- Chuan hoa toolchain sang JDK 25.
- Nang JavaFX, Mockito, JaCoCo, Surefire va Maven wrapper de tuong thich JDK 25.
- Doi Mockito sang inline mock maker va gan `mockito-core` bang Java agent trong Surefire.
- Them MySQL connector module vao `module-info.java`.
- Trong IntelliJ, chon Project SDK va Maven Runner JRE la JDK 25.

## 3. Nhung cho da sua

- `.mvn/wrapper/maven-wrapper.properties`: Maven wrapper `3.8.5` -> `3.9.15`.
- `pom.xml`: `maven.compiler.release=25`, JavaFX `25.0.2`, JUnit `5.13.4`, Mockito `5.23.0`, JaCoCo `0.8.14`, Surefire `3.5.4`, Compiler plugin `3.14.1`, Jar plugin `3.4.2`.
- `src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker`: `mock-maker-subclass` -> `mock-maker-inline`.
- `src/main/java/module-info.java`: them `requires mysql.connector.j;`, giu `requires java.sql;`.
- `.github/workflows/maven.yml`: CI dung `./mvnw -q clean install`.
- `README.md`: cap nhat yeu cau JDK 25.

## 4. Ket qua kiem tra

- `.\mvnw.cmd -q clean compile`: pass bang JDK 25.
- `.\mvnw.cmd -q test`: pass, 16/16 tests.
- `.\mvnw.cmd -q clean install`: pass khi Maven duoc phep ghi vao local repository `~/.m2`.

## 5. Ghi chu con lai

- Neu IntelliJ con bao `ms-21 is missing`, can doi SDK sang `C:\Program Files\Eclipse Adoptium\jdk-25.0.2.10-hotspot` va reimport Maven.
- Warning `package ExampleTest not in com.example.auctionprototype` khong lam fail build, nhung nen dua test vao package phu hop hoac xoa neu chi la test mau.
